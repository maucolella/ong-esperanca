import {templateInicio,templateProjetos,templateCadastro} from "./templates.js";import {buscarInteresses,salvarUltimaRota} from "./storage.js";const rotas={inicio:templateInicio,projetos:()=>templateProjetos(buscarInteresses()),cadastro:templateCadastro};export function obterRotaAtual() {return window.location.hash.replace("#", "")|| "inicio";} export function renderizarPagina({manterFoco=false}={}) {const app=document.getElementById("app");const anuncio=document.getElementById("anuncioRota");const rotaSolicitada=obterRotaAtual();const rota=rotas[rotaSolicitada]?rotaSolicitada: "inicio";app.innerHTML=rotas[rota]();salvarUltimaRota(`#${rota}`);document.querySelectorAll("[data-rota]").forEach(link=>{if (link.dataset.rota===rota) {link.setAttribute("aria-current", "page");} else {link.removeAttribute("aria-current");}});const titulos={inicio: "Início",projetos: "Projetos",cadastro: "Cadastro"};document.title= `${titulos[rota]} - ONG Esperança`;anuncio.textContent= `Página ${titulos[rota]} carregada.`;if (!manterFoco) {app.focus({preventScroll:true});window.scrollTo({top:0,behavior: "smooth"});}}
